<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <variable
            name="viewModel"
            type="com.sample.criminalintent.ui.viewmodel.ItemIntentViewModel" />
    </data>
<androidx.constraintlayout.widget.ConstraintLayout
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ui.view.ItemIntentFragment">


    <com.google.android.material.imageview.ShapeableImageView
        android:id="@+id/IntentPhoto"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp"
        android:scaleType="fitXY"
        app:layout_constraintDimensionRatio="1:1"
        app:layout_constraintEnd_toStartOf="@+id/DescriptionText"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="@+id/TitleText"
        tools:src="@tools:sample/avatars" />

    <EditText
        android:id="@+id/TitleText"
        android:text="@={viewModel.title}"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:layout_marginEnd="16dp"
        android:contentDescription="Title"
        android:ems="10"
        android:hint="Title"
        android:inputType="text"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <EditText
        android:id="@+id/DescriptionText"
        android:text="@={viewModel.description}"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:contentDescription="Description"
        android:ems="10"
        android:hint="Description"
        android:inputType="text"
        app:layout_constraintEnd_toEndOf="@+id/TitleText"
        app:layout_constraintTop_toBottomOf="@+id/TitleText" />


    <CheckBox
        android:id="@+id/SolvedCheckBox"
        android:checked="@={viewModel.isSolved()}"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:text="Intent Solved"
        app:layout_constraintEnd_toEndOf="@+id/DescriptionText"
        app:layout_constraintStart_toStartOf="@+id/DescriptionText"
        app:layout_constraintTop_toBottomOf="@+id/IntentPhoto" />

    <Button
        android:id="@+id/SetPhotoButton"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:text="Set Photo"
        app:layout_constraintBottom_toBottomOf="@+id/SolvedCheckBox"
        app:layout_constraintEnd_toEndOf="@+id/IntentPhoto"
        app:layout_constraintStart_toStartOf="@+id/IntentPhoto"
        app:layout_constraintTop_toTopOf="@+id/SolvedCheckBox" />

    <DatePicker
        android:id="@+id/datePicker"
        android:layout_width="wrap_content"
        android:layout_height="0dp"
        android:layout_marginTop="16dp"
        android:layout_marginBottom="32dp"
        android:backgroundTint="@color/red_500"
        android:headerBackground="@color/red_500"
        android:year="@={viewModel.year}"
        android:month="@={viewModel.month}"
        android:day="@={viewModel.day}"
        app:layout_constraintBottom_toTopOf="@+id/SaveButton"
        app:layout_constraintEnd_toEndOf="@+id/DescriptionText"
        app:layout_constraintStart_toStartOf="@+id/IntentPhoto"
        app:layout_constraintTop_toBottomOf="@+id/SetPhotoButton" />


    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/SaveButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="16dp"
        android:layout_marginBottom="32dp"
        android:contentDescription="Save Report"
        android:onClick="@{viewModel.saveIntent}"
        android:src="@drawable/ic_save"
        android:backgroundTint="@color/red_500"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />


</androidx.constraintlayout.widget.ConstraintLayout>
</layout>